<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lab.mapper.CategoryMapper">

    <select id="findAll" resultType="com.lab.entity.Category">
        SELECT * FROM category ORDER BY sort DESC
    </select>

    <select id="findById" resultType="com.lab.entity.Category">
        SELECT * FROM category WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, sort)
        VALUES (#{name}, #{sort})
    </insert>

    <delete id="deleteById">
        DELETE FROM category WHERE id = #{id}
    </delete>

</mapper>